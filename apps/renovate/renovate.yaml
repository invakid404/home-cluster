apiVersion: batch/v1
kind: CronJob
metadata:
  name: renovate
  namespace: apps
spec:
  schedule: "*/10 * * * *"
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        spec:
          securityContext:
            runAsGroup: 0
            runAsUser: 0
          containers:
            - name: renovate
              image: renovate/renovate:32.222.3@sha256:daf8e6960624a3740c1d2150b053a5dfffc80d800d124d997f3679f03e25fdaf
              command: ["sh"]
              args:
                [
                  "-c",
                  "usermod --uid ${SQUASH_UID} ubuntu && su -p ubuntu -c 'renovate invakid404/home-cluster'",
                ]
              env:
                - name: LOG_LEVEL
                  value: debug
              envFrom:
                - secretRef:
                    name: renovate-env
              volumeMounts:
                - mountPath: /data/cache
                  name: cache
              resources:
                limits:
                  cpu: 0.5
          restartPolicy: Never
          volumes:
            - name: cache
              persistentVolumeClaim:
                claimName: renovate-cache
