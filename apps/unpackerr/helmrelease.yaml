---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: unpackerr
  namespace: apps
spec:
  interval: 10m0s
  chart:
    spec:
      chart: app-template
      version: 1.2.1
      interval: 10m0s
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: apps

  values:
    image:
      repository: ghcr.io/onedr0p/unpackerr
      tag: 0.10.1@sha256:8950b556d980285d13bb851199354f441d79454e0534ba1c0dd738ec4c92b1b0

    env:
      TZ: "${TIMEZONE}"
      UN_SONARR_0_URL: https://sonarr.lab.invak.id
      UN_SONARR_0_PATHS_0: /data/nas-torrents/Downloads/Series
      UN_SONARR_0_API_KEY:
        secretKeyRef:
          name: sonarr
          key: api-key
      UN_RADARR_0_URL: https://radarr.lab.invak.id
      UN_RADARR_0_PATHS_0: /data/nas-torrents/Downloads/Movies
      UN_RADARR_0_API_KEY:
        secretKeyRef:
          name: radarr
          key: api-key
      UN_RADARR_1_URL: https://radarr-4k.lab.invak.id
      UN_RADARR_1_PATHS_0: "/data/nas-torrents/Downloads/Movies (4K)"
      UN_RADARR_1_API_KEY:
        secretKeyRef:
          name: radarr-4k
          key: api-key

    service:
      main:
        enabled: false

    probes:
      liveness:
        enabled: false
      readiness:
        enabled: false
      startup:
        enabled: false

    podSecurityContext:
      runAsUser: ${SQUASH_UID}
      runAsGroup: ${SQUASH_UID}
      fsGroup: ${SQUASH_UID}

    persistence:
      data:
        enabled: true
        type: nfs
        server: "${NAS_HOSTNAME}"
        path: "${TORRENTS_VOLUME}"
        mountPath: /data/nas-torrents

    resources:
      requests:
        cpu: 10m
        memory: 250Mi
      limits:
        memory: 1000Mi
